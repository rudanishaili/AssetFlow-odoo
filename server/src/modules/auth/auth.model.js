/**
 * Note: Prisma ORM automatically generates types and schemas based on `schema.prisma`.
 * This file serves as a reference model overview for the User entity.
 * 
 * User Fields:
 * - id: String (UUID) - Primary Key
 * - email: String - Unique
 * - password: String (Hashed)
 * - name: String
 * - role: Role (ADMIN, MANAGER, EMPLOYEE, AUDITOR)
 * - department: String (Optional)
 * - createdAt: Date
 * - updatedAt: Date
 */

export default {};
